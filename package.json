{
  "name": "titlepulse",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Title-only website keyword analysis with a Manifest V3 extension",
  "engines": {
    "node": ">=22.15"
  },
  "scripts": {
    "start": "node --env-file-if-exists=.env backend/server.js",
    "dev": "node --watch --env-file-if-exists=.env backend/server.js",
    "test": "node --test",
    "check": "node --check backend/server.js && node --check backend/crawler.js && node --check backend/network.js && node --check backend/analyzer.js && node --check backend/title-text.js && node --check backend/long-tail.js && node --check backend/export.js && node --check extension/popup.js"
  },
  "dependencies": {
    "axios": "^1.12.0",
    "cheerio": "^1.1.2",
    "exceljs": "^4.4.0",
    "express": "^5.1.0",
    "fast-xml-parser": "^5.2.5",
    "ipaddr.js": "^2.2.0",
    "robots-parser": "^3.0.1"
  },
  "overrides": {
    "exceljs": { "uuid": "^11.1.1" }
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
